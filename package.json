{
  "name": "safex",
  "bin": {
    "safex": "bin/safex.mjs"
  },
  "scripts": {
    "setup": "bun ./scripts/setup-safex.ts",
    "safex": "bun ./bin/safex.mjs",
    "init": "cmake -S . -B build",
    "build": "vite build",
    "predev": "cmake --build build --target sdl3js --parallel",
    "dev": "vite build --watch",
    "icons": "bun scripts/generate-icons.ts"
  }
}
